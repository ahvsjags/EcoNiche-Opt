# External Data Layer

This directory indexes public external resources used by EcoNiche-Opt.

Large downloaded files remain in `data/raw/` to avoid duplicate copies. The manifest files here record external source, local raw path, file size, checksum where practical, and downstream use.

Controlled or licensed datasets are not stored here. They remain documented as `ACCESS_RESTRICTED` in `config/data_registry.yml` and audit outputs.
